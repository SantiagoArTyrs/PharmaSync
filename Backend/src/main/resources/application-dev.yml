spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pharma
    username: pharma
    password: pharma123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  data:
    mongodb:
      uri: mongodb://localhost:27017/chatdb
    redis:
      host: localhost
      port: 6379
  n8n:
    webhook:
      url: http://localhost:5678/webhook/agent

logging:
  level:
    ROOT: INFO

app:
  name: PharmaSync
